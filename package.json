{
  "name": "hustlemode-ai",
  "version": "1.0.0",
  "description": "AI-powered accountability assistant with omnichannel support",
  "private": true,
  "scripts": {
    "dev": "cd src/api && uvicorn main:app --reload",
    "start": "cd src/api && uvicorn main:app",
    "test": "pytest tests/",
    "lint": "flake8 src/"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "HustleMode.ai",
  "license": "MIT"
} 